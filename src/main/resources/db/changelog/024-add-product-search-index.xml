<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
  xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="
        http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.0.xsd">

  <!-- Enable unaccent and pg_trgm extensions -->
  <changeSet id="024-enable-unaccent-and-trgm" author="ecommerce">
    <sql>CREATE EXTENSION IF NOT EXISTS unaccent</sql>
    <sql>CREATE EXTENSION IF NOT EXISTS pg_trgm</sql>
  </changeSet>

  <!-- Create IMMUTABLE f_unaccent function -->
  <changeSet id="024-f-unaccent" author="ecommerce">
    <sql splitStatements="false"><![CDATA[
      CREATE OR REPLACE FUNCTION f_unaccent(text) RETURNS text
        LANGUAGE sql IMMUTABLE PARALLEL SAFE STRICT AS
      $func$ SELECT public.unaccent('public.unaccent', $1) $func$
    ]]></sql>
  </changeSet>

  <!-- Add search_vec tsvector column to products and create GIN index -->
  <changeSet id="024-add-product-search-vec" author="ecommerce">
    <sql>ALTER TABLE products DROP COLUMN IF EXISTS search_vec</sql>
    <sql>
      ALTER TABLE products ADD COLUMN search_vec tsvector
        GENERATED ALWAYS AS (
          setweight(to_tsvector('simple', f_unaccent(coalesce(name, ''))), 'A') ||
          setweight(to_tsvector('simple', f_unaccent(coalesce(description, ''))), 'C')
        ) STORED
    </sql>
    <sql>CREATE INDEX IF NOT EXISTS idx_products_search_vec ON products USING gin (search_vec)</sql>
  </changeSet>

  <!-- Trigram index on unaccented product name for fast LIKE search -->
  <changeSet id="024-product-name-unaccent-trgm" author="ecommerce">
    <sql>CREATE INDEX IF NOT EXISTS idx_products_name_unaccent_trgm ON products USING gin (f_unaccent(lower(name)) gin_trgm_ops)</sql>
  </changeSet>

</databaseChangeLog>
